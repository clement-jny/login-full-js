//import React from 'react';
//import { createRoot, render} 
import ReactDOM from 'react-dom/client';
import { HomePage } from './home/homePage';

ReactDOM.createRoot(document.getElementById('root')).render(<HomePage />);